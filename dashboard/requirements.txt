# Module 4 — Dashboard SIEM Africa
# Dépendances Python (installer avec : pip install -r requirements.txt)

Django>=5.2,<5.3          # Framework web (version LTS, support long terme)
argon2-cffi>=23.1.0       # Hachage des mots de passe (argon2id)
reportlab>=4.0            # Génération des rapports PDF
openpyxl>=3.1             # Génération des rapports Excel

# Production (installé automatiquement par install_dashboard.sh) :
# gunicorn>=21.2.0        # Serveur WSGI
